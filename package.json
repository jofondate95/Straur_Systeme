{
  "name": "straur-systeme",
  "version": "1.0.0",
  "description": "STraur - Système de gestion intelligent de transport urbain",
  "author": "STraur Team",
  "license": "MIT",
  "scripts": {
    "install:all": "npm install && cd backend && npm install && cd ../mobile && npm install && cd ../web-admin && npm install && cd ../terminal-pwa && npm install",
    "dev:backend": "cd backend && npm run start:dev",
    "dev:mobile": "cd mobile && npm start",
    "dev:admin": "cd web-admin && npm start",
    "dev:terminal": "cd terminal-pwa && npm start",
    "build:backend": "cd backend && npm run build",
    "build:mobile": "cd mobile && eas build --platform ios android",
    "build:admin": "cd web-admin && npm run build",
    "build:terminal": "cd terminal-pwa && npm run build",
    "docker:up": "docker-compose up -d",
    "docker:down": "docker-compose down",
    "docker:logs": "docker-compose logs -f",
    "db:seed": "cd backend && npm run prisma:seed"
  },
  "workspaces": [
    "backend",
    "mobile",
    "web-admin",
    "terminal-pwa"
  ]
}
